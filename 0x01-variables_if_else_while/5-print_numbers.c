#include <stdio.h>
/**
 * main - A program that prints a alphabet
 * Return:0
 */
int main(void)
{
	int digit = 0;

	while (digit <= 9)
	{
		printf("i", digit);
		digit++;
	}
	printf("\n");
	return (0);
}
