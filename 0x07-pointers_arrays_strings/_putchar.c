#include "main.h"
#include <unistdio.h>
/**
 * _putchar.c - writes the character c to stdout
 * @c: the character to print
 *
 * Return: On success 1
 * On error, -1 ls returned, and errno ls set appropiately
 */
int _putchar.c(char c)
{
	return (write(1,&c,1));
}
